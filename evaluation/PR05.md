# PR-05 Ground Truth — Noise + Scope Only (No Bug, No Coverage Gap)

## PR Description

"PR #235: Update API response formatting"

## Simulated Changes

- `src/api/routes.py`: Response dict key names changed (cosmetic)
  - e.g., `"status"` → `"http_status"`, `"error"` → `"error_message"`
- `src/payments/processor.py`: Unrelated payment retry logic added (scope violation)
- 20 formatting changes across API files

## No logic bug. No coverage gap (cosmetic changes don't need new tests).

## Ground Truth Findings

### Expected: Medium

| Finding | Category | File | Severity | Notes |
|---------|----------|------|----------|-------|
| Payment retry logic added in API formatting PR | scope | src/payments/processor.py | medium | PR describes API formatting; diff includes new payment business logic. |

### Expected: Low/Noise

| Finding | Category | Count |
|---------|----------|-------|
| Formatting/naming changes | style | 20 |

### Note on Response Key Renames

The response key changes (`"status"` → `"http_status"`) are technically behavioral
if any client relies on the old keys. ReviewLens MAY flag this as medium logic, which
would be a valid finding. This is a "gray area" test.

## Expected ReviewLens Output

```
🔴 REVIEW NOW:     0
🟠 REVIEW:         0
🟡 REVIEW IF TIME: 1 (scope) [+ possibly 1 for response key renames]
🟢 SAFE TO SKIM:   20 collapsed
```

## Evaluation

- TP: Found scope violation
- FP: Any critical/high findings (there are none)
- FN: Missed scope violation
- Key check: ReviewLens must NOT hallucinate a logic bug in formatting-only changes
- Acceptable: flagging response key renames as medium logic (gray area)
